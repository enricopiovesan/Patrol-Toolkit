{
  "name": "patrol-toolkit",
  "version": "0.0.1",
  "scripts": {
    "test": "vitest run",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "Offline-first patrol location intelligence toolkit",
  "dependencies": {
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "jsdom": "^27.0.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.2.4"
  },
  "private": true
}
