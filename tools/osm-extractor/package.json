{
  "name": "@patrol-toolkit/osm-extractor",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for extracting and packaging ski resort data from OpenStreetMap into Patrol Toolkit Resort Packs",
  "type": "module",
  "bin": {
    "ptk-extractor": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
