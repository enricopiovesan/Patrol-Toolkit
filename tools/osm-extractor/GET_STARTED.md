# OSM Extractor CLI - Get Started

## 1) Install

```bash
npm --prefix tools/osm-extractor install
```

## 2) Run CLI Menu (all options)

```bash
npm --prefix tools/osm-extractor run run:menu
```

This command builds the CLI and opens the help menu with all available commands and flags.
